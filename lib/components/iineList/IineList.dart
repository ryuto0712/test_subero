import '../importer.dart';

class IineList extends StatelessWidget {
  IineList();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('いいね一覧'),
      ),
      body: Container(),
    );
  }
}
