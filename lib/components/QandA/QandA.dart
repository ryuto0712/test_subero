import '../importer.dart';

class QandA extends StatelessWidget {
  QandA();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('よくある質問'),
      ),
      body: Container(),
    );
  }
}
