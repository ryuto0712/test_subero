import '../../importer.dart';

class AccountConfig extends StatelessWidget {
  AccountConfig();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('アカウント設定'),
      ),
      body: Container(),
    );
  }
}
